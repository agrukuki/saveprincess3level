#include "Princess.h"

void Princess::update()
{
	/*
	if (this->getPosition().x <= 0 || this->getPosition().x>=320-32)
	{
		this->dX = -this->dX;
	}
	this->move(this->dX, this->dY);
	*/
}



Princess::Princess(string name) : Character(name)
{
	//this->dX = -0.1;
	
}
